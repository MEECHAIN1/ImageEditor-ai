import React, { useState, useEffect } from 'react';
import { 
  Compass, 
  Wallet, 
  Zap, 
  ShieldCheck, 
  Database, 
  Award, 
  ChevronRight, 
  Info,
  ExternalLink,
  Bot,
  LayoutDashboard,
  Image as ImageIcon,
  History,
  Settings,
  Plus,
  ArrowRight
} from 'lucide-react';

// --- Mock Services & Hooks ---
const useWallet = () => {
  const [connectedAccount, setConnectedAccount] = useState(null);
  const [network, setNetwork] = useState({ name: 'MeeChain Mainnet', chainId: 1337 });
  const [isCorrectNetwork, setIsCorrectNetwork] = useState(true);

  const connectWallet = () => setConnectedAccount("0x71C7...E921");
  const switchNetwork = () => setIsCorrectNetwork(true);

  return { connectedAccount, connectWallet, isCorrectNetwork, switchNetwork, network, provider: {} };
};

const SUPPORTED_NETWORKS = {
  MEECHAIN: { name: 'MeeChain', chainId: 1337 }
};

// --- Sub-Components ---

const MeeBotWidget = ({ mood, message }) => (
  <div className="bg-white border-4 border-black p-6 rounded-3xl shadow-[8px_8px_0px_0px_rgba(0,0,0,1)] flex items-center gap-6 mb-8 overflow-hidden relative group">
    <div className="absolute top-0 right-0 p-2 opacity-10 group-hover:scale-110 transition-transform">
      <Bot size={80} />
    </div>
    <div className="w-20 h-20 bg-[#00f0dc] rounded-2xl flex items-center justify-center shrink-0 border-4 border-black animate-bounce">
      <Bot className="w-10 h-10" />
    </div>
    <div className="relative z-10">
      <p className="font-black italic text-sm text-cyan-600 uppercase mb-1">MeeBot Status: {mood}</p>
      <p className="font-bold text-lg leading-tight">{message}</p>
    </div>
  </div>
);

const NFTGallery = ({ connectedAccount }) => (
  <div className="bg-white border-4 border-black rounded-3xl p-6 shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] h-full">
    <div className="flex justify-between items-center mb-6">
      <h3 className="text-xl font-black italic uppercase flex items-center gap-2">
        <ImageIcon size={20} /> My NFTs
      </h3>
      <button className="text-xs font-bold underline">View All</button>
    </div>
    <div className="grid grid-cols-2 gap-4">
      {[1, 2].map(i => (
        <div key={i} className="aspect-[3/4] bg-gray-100 border-2 border-black rounded-xl overflow-hidden relative group">
          <div className="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent flex flex-col justify-end p-3 text-white">
            <p className="text-[10px] font-bold opacity-70">Genesis Card</p>
            <p className="font-black text-sm">MEEBOT #{i}</p>
          </div>
          <div className="absolute top-2 right-2 w-6 h-6 bg-[#00f0dc] rounded-full border-2 border-black flex items-center justify-center">
            <Zap size={10} className="fill-current" />
          </div>
        </div>
      ))}
      <button className="aspect-[3/4] border-2 border-dashed border-gray-300 rounded-xl flex flex-col items-center justify-center gap-2 hover:bg-gray-50 transition-colors">
        <Plus className="text-gray-400" />
        <span className="text-[10px] font-bold text-gray-400 uppercase">Mint New</span>
      </button>
    </div>
  </div>
);

const BadgeGallery = () => (
  <div className="bg-black text-white rounded-3xl p-6 h-full">
    <h3 className="text-lg font-black italic uppercase mb-4 flex items-center gap-2">
      <Award size={20} className="text-[#00f0dc]" /> Badges
    </h3>
    <div className="flex flex-wrap gap-3">
      {['Early Bird', 'NFT Pro', 'Voter'].map((b, i) => (
        <div key={i} className="w-12 h-12 bg-white/10 rounded-full flex items-center justify-center border border-white/20 hover:bg-[#00f0dc] hover:text-black transition-all cursor-help" title={b}>
          <Zap size={20} />
        </div>
      ))}
    </div>
  </div>
);

const App = () => {
  const { connectedAccount, connectWallet, isCorrectNetwork, switchNetwork, network } = useWallet();
  const [activeTab, setActiveTab] = useState('dashboard');

  const truncateAddress = (addr) => `${addr.slice(0, 6)}...${addr.slice(-4)}`;

  return (
    <div className="min-h-screen bg-[#00f0dc] font-sans selection:bg-cyan-200" 
         style={{ backgroundImage: `url("https://www.transparenttextures.com/patterns/robots.png")` }}>
      
      {/* Navigation */}
      <nav className="bg-white border-b-4 border-black sticky top-0 z-50 p-4">
        <div className="max-w-7xl mx-auto flex justify-between items-center">
          <div className="flex items-center gap-6">
            <div className="flex items-center gap-2">
              <div className="w-10 h-10 bg-black rounded-xl flex items-center justify-center">
                <Zap className="text-[#00f0dc] fill-current" />
              </div>
              <span className="font-black text-2xl tracking-tighter italic">MEECHAIN</span>
            </div>
            
            <div className="hidden md:flex gap-1">
              {['Dashboard', 'Market', 'Missions', 'Governance'].map(tab => (
                <button 
                  key={tab}
                  onClick={() => setActiveTab(tab.toLowerCase())}
                  className={`px-4 py-2 text-xs font-black uppercase tracking-widest rounded-lg transition-all ${
                    activeTab === tab.toLowerCase() ? 'bg-black text-white' : 'hover:bg-black/5'
                  }`}
                >
                  {tab}
                </button>
              ))}
            </div>
          </div>

          <div className="flex gap-4 items-center">
            {connectedAccount ? (
              <div className="flex items-center gap-3">
                <div className="text-right hidden sm:block">
                  <p className="text-[10px] font-black uppercase text-gray-400 leading-none">{network.name}</p>
                  <p className="text-sm font-bold leading-tight">{truncateAddress(connectedAccount)}</p>
                </div>
                <div className="w-10 h-10 rounded-full bg-black border-2 border-white shadow-lg flex items-center justify-center overflow-hidden">
                   <Bot className="text-[#00f0dc]" size={20} />
                </div>
              </div>
            ) : (
              <button 
                onClick={connectWallet}
                className="bg-black text-white px-6 py-2 rounded-xl font-black text-sm shadow-[4px_4px_0px_0px_rgba(0,0,0,0.2)] hover:translate-y-1 hover:shadow-none transition-all"
              >
                CONNECT WALLET
              </button>
            )}
          </div>
        </div>
      </nav>

      <main className="max-w-7xl mx-auto p-6 md:p-8">
        
        {!connectedAccount ? (
          <div className="min-h-[70vh] flex flex-col items-center justify-center text-center">
            <div className="w-32 h-32 bg-white rounded-[40px] border-4 border-black flex items-center justify-center mb-8 shadow-[12px_12px_0px_0px_rgba(0,0,0,1)] animate-bounce">
              <Bot size={64} />
            </div>
            <h1 className="text-5xl font-black italic uppercase mb-4 tracking-tighter">Welcome to MeeChain</h1>
            <p className="text-xl font-bold text-gray-700 max-w-lg mb-8">
              Connect your wallet to explore your NFT collection, badges, and governance power.
            </p>
            <button 
              onClick={connectWallet}
              className="bg-black text-[#00f0dc] px-12 py-5 rounded-2xl font-black text-xl shadow-[8px_8px_0px_0px_rgba(0,0,0,0.2)] hover:scale-105 active:scale-95 transition-all flex items-center gap-3"
            >
              START YOUR JOURNEY <ArrowRight />
            </button>
          </div>
        ) : (
          <div className="grid grid-cols-1 xl:grid-cols-4 gap-8">
            
            {/* Main Content Area */}
            <div className="xl:col-span-3 space-y-8">
              
              {!isCorrectNetwork && (
                <div className="bg-red-500 text-white p-4 rounded-2xl border-4 border-black flex justify-between items-center shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]">
                  <div className="flex items-center gap-3">
                    <Info />
                    <span className="font-bold uppercase italic">Wrong Network Detected!</span>
                  </div>
                  <button onClick={switchNetwork} className="bg-white text-black px-4 py-1 rounded-lg font-black text-xs hover:bg-gray-100">
                    SWITCH TO MEECHAIN
                  </button>
                </div>
              )}

              <MeeBotWidget 
                mood="Joyful" 
                message="ยินดีต้อนรับกลับครับ! วันนี้มีภารกิจใหม่ 'Genesis Mint' พร้อมให้คุณสะสมแล้ว สนใจเริ่มเลยไหมครับ?" 
              />

              <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
                <NFTGallery connectedAccount={connectedAccount} />
                
                <div className="space-y-8">
                  <BadgeGallery />
                  <div className="bg-white border-4 border-black rounded-3xl p-6 shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]">
                    <h3 className="text-lg font-black italic uppercase mb-4 flex items-center gap-2">
                       <History size={20} /> My Journey
                    </h3>
                    <div className="space-y-4">
                       {[
                         { t: 'Account Created', d: '2 days ago', icon: <Plus size={12}/> },
                         { t: 'First NFT Minted', d: '1 day ago', icon: <Zap size={12}/> }
                       ].map((item, i) => (
                         <div key={i} className="flex gap-4 items-start relative pb-4 last:pb-0">
                           {i !== 1 && <div className="absolute left-[9px] top-6 bottom-0 w-0.5 bg-gray-200"></div>}
                           <div className="w-5 h-5 rounded-full bg-black text-[#00f0dc] flex items-center justify-center shrink-0 z-10">
                             {item.icon}
                           </div>
                           <div>
                             <p className="font-bold text-sm leading-none">{item.t}</p>
                             <p className="text-[10px] font-bold text-gray-400 uppercase mt-1">{item.d}</p>
                           </div>
                         </div>
                       ))}
                    </div>
                  </div>
                </div>
              </div>

              {/* Proposals Preview */}
              <div className="bg-white border-4 border-black rounded-3xl p-8 shadow-[8px_8px_0px_0px_rgba(0,0,0,1)]">
                <div className="flex justify-between items-end mb-8">
                  <div>
                    <h2 className="text-3xl font-black italic uppercase tracking-tighter">Governance</h2>
                    <p className="font-bold text-gray-500">Vote on the future of MeeChain ecosystem.</p>
                  </div>
                  <button className="bg-black text-white px-6 py-2 rounded-xl font-black text-xs">VIEW ALL PROPOSALS</button>
                </div>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div className="p-4 border-2 border-black rounded-2xl hover:bg-gray-50 transition-colors">
                    <div className="flex justify-between mb-2">
                       <span className="px-2 py-0.5 bg-green-100 text-green-700 text-[10px] font-black rounded uppercase tracking-widest">Active</span>
                       <span className="text-[10px] font-bold text-gray-400">Ends in 2d</span>
                    </div>
                    <h4 className="font-bold mb-4 leading-tight">Implement Layer 2 Bridge for NFT Interoperability</h4>
                    <div className="w-full bg-gray-100 h-2 rounded-full overflow-hidden">
                       <div className="bg-[#00f0dc] h-full w-2/3 border-r border-black"></div>
                    </div>
                  </div>
                  <div className="p-4 border-2 border-black rounded-2xl hover:bg-gray-50 transition-colors">
                    <div className="flex justify-between mb-2">
                       <span className="px-2 py-0.5 bg-green-100 text-green-700 text-[10px] font-black rounded uppercase tracking-widest">Active</span>
                       <span className="text-[10px] font-bold text-gray-400">Ends in 5d</span>
                    </div>
                    <h4 className="font-bold mb-4 leading-tight">Increase Staking Rewards for Early MeeBot Holders</h4>
                    <div className="w-full bg-gray-100 h-2 rounded-full overflow-hidden">
                       <div className="bg-[#00f0dc] h-full w-1/4 border-r border-black"></div>
                    </div>
                  </div>
                </div>
              </div>

            </div>

            {/* Sidebar Stats */}
            <div className="space-y-6">
              <div className="bg-white border-4 border-black rounded-3xl p-6 shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]">
                <h3 className="text-xs font-black uppercase text-gray-400 mb-4 tracking-widest">Network Stats</h3>
                <div className="space-y-4">
                  <div>
                    <p className="text-[10px] font-bold text-gray-500 uppercase">Total Value Locked</p>
                    <p className="text-2xl font-black italic">$1,240,500</p>
                  </div>
                  <div>
                    <p className="text-[10px] font-bold text-gray-500 uppercase">MeeBot Population</p>
                    <p className="text-2xl font-black italic">12,450</p>
                  </div>
                  <div>
                    <p className="text-[10px] font-bold text-gray-500 uppercase">Gas Price</p>
                    <p className="text-xl font-black italic text-cyan-600">0.001 MEE</p>
                  </div>
                </div>
              </div>

              <div className="bg-purple-600 border-4 border-black rounded-3xl p-6 shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] text-white">
                <h3 className="text-lg font-black italic uppercase mb-2">AI Image Editor</h3>
                <p className="text-xs font-bold opacity-80 mb-4 leading-relaxed">Customize your MeeBot NFTs using our generative AI engine.</p>
                <button className="w-full bg-white text-black py-3 rounded-xl font-black text-xs shadow-[4px_4px_0px_0px_rgba(0,0,0,0.3)] hover:translate-y-1 hover:shadow-none transition-all">
                  LAUNCH EDITOR
                </button>
              </div>

              <div className="p-4 rounded-2xl border-2 border-black/10 text-center">
                 <p className="text-[10px] font-bold text-gray-400 uppercase">Powered by</p>
                 <p className="font-black italic tracking-tighter opacity-20 text-2xl">MEECHAIN</p>
              </div>
            </div>

          </div>
        )}

      </main>

      {/* Floating Action Button / Station */}
      <div className="fixed bottom-8 right-8 group">
         <div className="absolute bottom-full right-0 mb-4 w-64 bg-white border-4 border-black rounded-2xl p-4 shadow-2xl opacity-0 translate-y-4 group-hover:opacity-100 group-hover:translate-y-0 transition-all">
            <p className="font-bold text-sm mb-2">MeeBot Assistant</p>
            <p className="text-xs text-gray-500 leading-relaxed">ต้องการความช่วยเหลือในการ Mint หรือใช้งานระบบ Governance ไหมครับ?</p>
            <button className="mt-3 w-full bg-black text-[#00f0dc] py-2 rounded-lg font-black text-[10px] uppercase">เปิดแชท</button>
         </div>
         <button className="w-16 h-16 bg-black rounded-full flex items-center justify-center border-4 border-white shadow-2xl hover:scale-110 transition-transform">
            <Bot className="text-[#00f0dc]" />
         </button>
      </div>

    </div>
  );
};

export default App;